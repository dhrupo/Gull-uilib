import { Typography } from "@material-ui/core";
import React from "react";

const file = () => {
  return (
    <Typography variant="h6" color="secondary">
      This is file page.
    </Typography>
  );
};

export default file;
