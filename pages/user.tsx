import { Typography } from "@material-ui/core";
import React from "react";

const user = () => {
  return (
    <Typography variant="h6" color="secondary">
      This is user page.
    </Typography>
  );
};

export default user;
