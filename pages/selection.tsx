import { Typography } from "@material-ui/core";
import React from "react";

const selection = () => {
  return (
    <Typography variant="h6" color="secondary">
      This is selection page.
    </Typography>
  );
};

export default selection;
